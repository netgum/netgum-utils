export interface IAnyToHexOptions {
  length?: number;
  add0x?: boolean;
  autoStringDetect?: boolean;
  evenLength?: boolean;
  defaults?: string;
}

export interface IPrepareHexOptions {
  evenLength?: boolean;
  add0x?: boolean;
}
